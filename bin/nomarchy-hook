#!/bin/bash

# Run a named hook from ~/.config/nomarchy/hooks/<name>.

set -e

if (( $# < 1 )); then
  echo "Usage: nomarchy-hook [name] [args...]"
  exit 1
fi

HOOK_PATH="$HOME/.config/nomarchy/hooks/$1"
shift

if [[ -f $HOOK_PATH ]]; then
  bash "$HOOK_PATH" "$@"
fi
